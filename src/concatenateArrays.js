function solution(a, b) {
    return a.concat(b);
}

module.exports = solution;

// npm init -y
// npm i jest-cli -g
// package.json: "scripts": { "test": "jest" }
// npm test concatenateArrays

// alternative solution
